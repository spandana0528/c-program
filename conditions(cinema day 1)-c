#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() 
{
    int day, month, year, students;
    int sum, product;
    scanf("%d", &day);
    scanf("%d", &month);
    scanf("%d", &year);
    scanf("%d", &students);
    sum = day + month + year;
    product = 3 * 4;
    if (sum % product == 0 && students > 50 && students < 100)
    {
        printf("Cinema Day\n");
    } 
    else 
    {
        printf("Not a Cinema Day\n");
    }

    return 0;
}

      
    
